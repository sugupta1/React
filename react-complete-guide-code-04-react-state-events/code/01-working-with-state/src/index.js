import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); //Render a React element into the DOM in the supplied 
//container and return a reference to the component (or returns null for stateless components)
